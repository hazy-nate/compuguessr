
server.modules          += ( "mod_fastcgi" )
server.port             = 8081
server.document-root    = "/var/www/html"

fastcgi.server = (
    "/" => ((
        "host" => "127.0.0.1",
        "port" => 9000,
        "check-local" => "disable"
    ))
)
