
server.modules          += ( "mod_fastcgi" )
server.port             = 8081
server.document-root    = "/var/www/html"

fastcgi.server = (
    "/" => ((
        "bin-path"    => "/home/nate/projects/code/rio-compuguessr/compuguessr/bin/debug/compuguessr",
        "socket"      => "/tmp/compuguessr.sock",
        "max-procs"   => 1,
        "check-local" => "disable"
    ))
)
